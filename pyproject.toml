[project]
name = "pishro-cli"
version = "0.1.0"
description = "Pishro is a deployment tool for docker swarm"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "pishro-lib",
    "typer>=0.15.2",
]

[project.scripts]
pishro = "pishro_cli.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
pishro-lib = { git = "https://github.com/siavashoutadi/pishro-lib" }
